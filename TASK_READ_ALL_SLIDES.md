# TASK_READ_ALL_SLIDES.md — 把 102 张幻灯片逐张「看」成文字

> ## 任务目标
>
> 用 `functions.shell` 的 `image_path`，**逐张读取 102 张幻灯片**，
> 把每张图上的**全部信息**（尤其是 OCR 抓不到的手写、颜色、结构、插图）写成文字。
>
> **产出**：`out/slides_vision.md` —— 这是后续制作学习资料的核心输入。
>
> ---
> ## ⚠️ 三条铁律（上一轮测试踩过的坑）
>
> **① 严格按顺序，不许跳、不许倒序。**
> 必须 `S001 → S002 → S003 → … → S102`。
> **上一轮它写成 `S001 → S010 → 总结 → S009 → S008 → …`，顺序全乱了。**
>
> **② 每张必填「OCR 对照」。**
> 上一轮全部写了"未对照"，导致无法衡量读图价值。**这次必须填。**
>
> **③ 手写批注要尽量逐字抄录。**
> 上一轮写得太笼统（"可辨认'接受''机械'等"）。
> 这次要**把能认出来的字都写出来**；认不准的标「认不准」，**不许编**。

---

## 第零步 · 准备与断点续跑

```bash
# 同步最新代码
git fetch origin && git merge origin/main

# 解压（只需一次）
mkdir -p /tmp/rt && cd /tmp/rt
unzip -q /path/to/out/frames/slides_102.zip -d slides/
ls slides/*.jpg | wc -l          # 必须等于 102
```

**断点续跑**：检查 `out/slides_vision.md` 已有哪些 `## S###` 小节，
**从下一个编号接着读**。不要重头再来。

```bash
grep -o "^## S[0-9]*" out/slides_vision.md | tail -1   # 看读到哪了
```

**如果文件不存在** → 新建，从 S001 开始。

---

## 逐张读取（**一次一张**）

对每个 `S###.jpg`，用：

```
functions.shell(command="...", image_path="/tmp/rt/slides/S###.jpg")
```

**读完立刻追加写入** `out/slides_vision.md`，格式**严格**如下：

```markdown
## S001

**标题**：（图上最大的标题；没有就写"无"）
**页码角标**：（如 B81 / 讲义 P68；没有就写"无"）

**印刷内容**：（照抄图上的印刷文字。表格就按行列描述；图表就描述结构）

**手写批注**：
- 位置：（具体到哪个区域/哪一行/哪个格子）
- 颜色：（红 / 紫 / 蓝 / 绿 / 黑）
- 内容：（**尽量逐字抄录**。认不准的字写「认不准」，不要猜、不要跳过）

**颜色与高亮**：
- 高亮底色：哪一块？什么颜色？没有就写"无"
- 红笔标注：哪些内容是红笔？

**版式结构**：
- 类型：（纯文字 / 表格 / 树状图 / 流程图 / 对比图 / 其他）
- 结构描述：（行列数、层级、箭头方向、连线关系）

**插图与图标**：（画的是什么：猫 / 狗 / 铃铛 / 迷宫 / 手绘示意图 / 其他）

**OCR 对照**：⭐ 必填
- OCR 有、我没看到的：（没有就写"无"）
- **我看到、OCR 没有的**：（**这一栏最重要**，没有就写"无"）

---
```

**注意**：
- `---` 分隔线不能少（方便程序切分）
- **顺序追加**，不许插队
- **每读完 10 张就 `git add` + `git commit` 一次**，防上下文爆掉丢进度

---

## 关于「OCR 对照」怎么填

读图时**不要**提前看 OCR。**读完再对照**：

```bash
# 看某张的 OCR
python3 -c "
import re
s=open('out/slides_unique.txt',encoding='utf-8').read()
m=re.search(r'=== S006 \[[\d:]+\] ===([\s\S]*?)(?==== S007)', s)
print(m.group(1) if m else '未找到')
"
```

**然后逐条比对**，填两栏：
- `OCR 有、我没看到的` → 说明你漏看了，**回去补看**
- `我看到、OCR 没有的` → **这是你的核心价值**，手写/颜色/结构通常都在这一栏

---

## 读完之后 · 出一份「OCR 漏掉的信息汇总」

新建 `out/OCR_GAPS.md`，汇总**所有 102 张里，OCR 没有但你看到的信息**：

```markdown
# OCR 漏掉的信息汇总

## 一、手写批注（OCR 完全抓不到）

| 幻灯片 | 手写内容 | 颜色 | 位置 |
|---|---|---|---|
| S006 | 选项 C 打叉，旁注「人为赋予」 | 红 | 选项 C 右侧 |
| S023 | 「定义1：预备 = 动（动机）」 | 红 | 右下角 |
| … | | | |

## 二、颜色与高亮（OCR 无颜色信息）

| 幻灯片 | 内容 | 颜色 |
|---|---|---|

## 三、版式与结构（OCR 会把结构压平）

| 幻灯片 | 结构类型 | 说明 |
|---|---|---|
| S001 | 2 行 × 3 列矩阵 | OCR 压成散行，丢失行列归属 |
| … | | |

## 四、插图与图标

| 幻灯片 | 画的是什么 |
|---|---|

## 五、结论

- 共 102 张，其中 __ 张有 OCR 抓不到的信息
- 最有价值的是：____
```

---

## 自检（**读完后必须做**）

```bash
# 1. 读了几张？
grep -c "^## S" out/slides_vision.md        # 必须 = 102

# 2. 顺序对不对？（必须严格递增）
grep -o "^## S[0-9]*" out/slides_vision.md | sed 's/## //' | sort -c && echo "✅ 顺序正确" || echo "❌ 顺序乱了"

# 3. 有没有缺号？
grep -o "^## S[0-9]*" out/slides_vision.md | sed 's/## S//' > /tmp/got.txt
seq -f "%03g" 1 102 > /tmp/need.txt
comm -13 <(sort /tmp/got.txt) /tmp/need.txt      # 应该没有输出
```

**三项全部通过才算完成。**

---

## 汇报格式

| # | 项目 | 结果 |
|---|---|---|
| 1 | 读了 __ / 102 张 | |
| 2 | 顺序检查 | 通过 / 不通过 |
| 3 | 有缺号吗 | 无 / 缺：____ |
| 4 | 有几张抓到了 OCR 没有的信息 | __ 张 |
| 5 | 手写辨认准确度自评 | |
| 6 | 有没有中途中断、如何恢复的 | |

**最后一句话**：
> 我读完了 __ / 102 张，其中 __ 张抓到了 OCR 没有的信息。
> 完成 / 未完成，未完成的原因是 ____。

---

## 提交

```bash
git add -A
git commit -m "feat: 102 张幻灯片逐张读图记录 + OCR 漏掉的信息汇总"
git push -u origin HEAD
```

**只新增这两个文件**：`out/slides_vision.md`、`out/OCR_GAPS.md`
**不要改** `out/` 下其他任何文件。
