# TASK_READ_BATCH.md — 逐张读图（**分批版**）

> ## 🔴 本次只读「一个批次」，读完就停
>
> **你会在新会话里被要求读某一批。** 批次范围由用户在对话里告诉你，例如：
>
> ```
> 读 TASK_READ_BATCH.md，本次范围：S001~S015
> ```
>
> **读到批次末尾就停下** —— 写入、commit、汇报、结束。
>
> - ❌ **不要**继续读下一批（下一批会在**新会话**里跑）
> - ❌ **不要**试图一次读完 102 张（**上一轮就是这样失败的**：只读了 1 张就撑不住了）
>
> **如果本次仍然做不完，必须明确说清卡在哪**：
> 上下文长度？工具调用次数上限？超时？还是别的原因？
> **并说明你已经读到了第几张、写了多少条。**

---

## 为什么分批

一次性读 102 张会失败，原因是：
- 每张图的描述要写 300~500 字，102 张就是 3~5 万字
- 每张图都要调用一次工具，加上对照 OCR，工具调用次数很多
- 会话上下文会爆

**所以：每批 15 张，共 7 批。**

| 批次 | 范围 |
|---|---|
| 第 1 批 | S001 ~ S015 |
| 第 2 批 | S016 ~ S030 |
| 第 3 批 | S031 ~ S045 |
| 第 4 批 | S046 ~ S060 |
| 第 5 批 | S061 ~ S075 |
| 第 6 批 | S076 ~ S090 |
| 第 7 批 | S091 ~ S102 |

---

## 准备

```bash
# 同步最新代码（必须！工作区不会自动拉新文件）
git fetch origin && git merge origin/main

# 解压（如果 /tmp/rt/slides 不存在）
mkdir -p /tmp/rt && cd /tmp/rt
unzip -q /path/to/out/frames/slides_102.zip -d slides/
ls slides/*.jpg | wc -l          # 必须等于 102
```

**断点续跑**：先看 `out/slides_vision.md` 已有哪些小节，**不要重复读**。

```bash
grep -o "^## S[0-9]*" out/slides_vision.md 2>/dev/null | tail -3
```

---

## 逐张读取（**一次一张，严格顺序**）

用 `functions.shell` 的 `image_path` 参数：

```
functions.shell(command="ls slides/S011.jpg", image_path="/tmp/rt/slides/S011.jpg")
```

**读完立刻追加写入** `out/slides_vision.md`：

```markdown
## S011

**标题**：
**页码角标**：

**印刷内容**：（照抄印刷文字；表格按行列描述）

**手写批注**：
- 位置：
- 颜色：（红 / 紫 / 蓝 / 绿 / 黑）
- 内容：（**尽量逐字抄录**；认不准的字写「认不准」，不许猜）

**颜色与高亮**：
- 高亮底色：哪块？什么颜色？没有就写"无"
- 红笔标注：哪些是红笔？

**版式结构**：
- 类型：（纯文字 / 表格 / 树状图 / 流程图 / 对比图）
- 结构：（行列数、层级、箭头方向）

**插图与图标**：

**OCR 对照**：⭐ 必填
- OCR 有、我没看到的：
- **我看到、OCR 没有的**：

---
```

### 关于「OCR 对照」

**读完图再看 OCR**（不许提前看，否则会漏掉手写和颜色）：

```bash
python3 -c "
import re
s=open('out/slides_unique.txt',encoding='utf-8').read()
m=re.search(r'=== S011 \[[\d:]+\] ===([\s\S]*?)(?==== S012)', s)
print(m.group(1) if m else '未找到')
"
```

然后填两栏：`OCR 有我没看到的`（说明漏看了，回去补）+ `我看到 OCR 没有的`（**最重要**）。

---

## 本批完成后的动作

1. **自检本批**：
```bash
# 本批张数对不对
grep -c "^## S" out/slides_vision.md
# 顺序对不对
grep -o "^## S[0-9]*" out/slides_vision.md | sed 's/## //' | sort -c && echo "✅ 顺序正确" || echo "❌ 顺序乱"
```

2. **commit + push**：
```bash
git add out/slides_vision.md
git commit -m "feat: 幻灯片读图 第N批（SXXX~SYYY）"
git push -u origin HEAD
```

3. **汇报**：

| # | 项目 | 结果 |
|---|---|---|
| 1 | 本次范围 | S___ ~ S___ |
| 2 | 实际读了几张 | |
| 3 | 累计读到第几张 | |
| 4 | 顺序检查 | 通过 / 不通过 |
| 5 | 有几张抓到 OCR 没有的信息 | |
| 6 | 手写辨认准确度自评 | |
| 7 | **如果没做完，卡在哪**（必答） | |

**最后一句话**：
> 本次读完 __ 张（S___ ~ S___），累计 __ / 102。
> 本批**完成 / 未完成**，未完成是因为 ____。

---

## 全部 7 批跑完之后（最后一批做）

新建 `out/OCR_GAPS.md`，汇总所有批次里 **OCR 没有、但看图看到** 的信息：

```markdown
# OCR 漏掉的信息汇总

## 一、手写批注
| 幻灯片 | 手写内容 | 颜色 | 位置 |
|---|---|---|---|

## 二、颜色与高亮
| 幻灯片 | 内容 | 颜色 |
|---|---|---|

## 三、版式与结构
| 幻灯片 | 结构类型 | 说明 |
|---|---|---|

## 四、插图与图标
| 幻灯片 | 画的是什么 |
|---|---|

## 五、结论
共 102 张，其中 __ 张有 OCR 抓不到的信息。
```
